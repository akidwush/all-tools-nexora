"use strict";const M=require("../provider-models").recraft,{dimensions,timedFetch,responseError}=require("../utils");module.exports={id:"recraft",name:"Recraft",envKey:"RECRAFT_API_KEY",model:M.model,capabilities:{textToImage:true,aspectRatio:true,customStyle:true},async generate(i,c={}){const d=dimensions(i.aspectRatio),p={model:M.model,prompt:i.prompt,n:i.variations||1,size:`${d.width}x${d.height}`,response_format:"url"},sid=i.useNexoraStyle?String(process.env.RECRAFT_NEXORA_COVER_STYLE_ID||"").trim():"";if(sid)p.style_id=sid;const r=await timedFetch(M.endpoint,{method:"POST",headers:{Authorization:`Bearer ${c.apiKey}`,"Content-Type":"application/json"},body:JSON.stringify(p)},c.timeoutMs,c.fetch);if(!r.ok)await responseError(r);const j=await r.json();return{model:M.model,requestId:j.id,images:(j.data||[]).map(x=>({url:x.url,width:d.width,height:d.height}))}}};

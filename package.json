{
  "name": "all-tools-nexora-developer-dika",
  "version": "4.2.0",
  "private": true,
  "description": "All Tools Nexora v4.2 — modular lazy loading, live audit, and persistent tool health monitoring",
  "scripts": {
    "dev": "node serve-local.js",
    "start": "node serve-local.js",
    "check": "node scripts/check-project.js",
    "build": "node scripts/build.js",
    "test": "node scripts/test-api.js && node scripts/test-audit.js && node scripts/test-tool-health.js"
  },
  "engines": {
    "node": ">=18"
  }
}
